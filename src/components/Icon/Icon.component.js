import { createIconSetFromFontello } from 'react-native-vector-icons';

import { config, name } from '../../assets/fontello';

const IconSet = createIconSetFromFontello(config, name);

export default IconSet;
