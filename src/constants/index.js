import { displayName as APP_NAME } from '../../app';
import TEST_IDS from './TestID.constants';
import TestIDConstantsUtils from './TestID.constants.utils';

const constants = {
  APP_NAME,
  TEST_IDS,
};

export default constants;
export { TestIDConstantsUtils };
